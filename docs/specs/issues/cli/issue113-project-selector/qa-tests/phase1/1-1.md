# QAテスト仕様書 - タスクグループ 1.1: プロジェクト選択フロー検証

**Issue**: #113 vibe-kanban v0.1.18 対応
**タスクグループ ID**: 1.1
**対象ファイル**: `packages/cli/src/commands/task-loop/lib/project-selector.ts`
**テストファイル**: `packages/cli/src/commands/task-loop/lib/project-selector.test.ts`
**実施日**: 2026-02-24
**ステータス**: SUCCESS

---

## 受け入れ条件と対応テストケース

### タスク 1.1.1: 設定ファイルが存在しない場合のプロジェクト選択UI

| # | テストケース | 期待動作 | 結果 |
|---|------------|---------|------|
| 1 | `.vibe-kanban.json` が存在しない場合、プロジェクト選択のinquirerプロンプトが表示される | inquirer.prompt が `projectId` を name に持つリスト型プロンプトで呼ばれる | ✅ |
| 2 | `.vibe-kanban.json` が存在しない場合、listOrganizations が呼ばれる | listOrganizations が1回呼ばれる | ✅ |
| 3 | 組織が1つの場合、組織選択UIは表示されずに自動選択される | listProjects が自動選択された orgId で呼ばれる | ✅ |

### タスク 1.1.2: 「新しいプロジェクトを作成」選択時のプロジェクト作成フロー

| # | テストケース | 期待動作 | 結果 |
|---|------------|---------|------|
| 4 | 「新しいプロジェクトを作成」を選択すると、VibeKanbanRestClient でプロジェクトが作成される | createProject が名前と process.cwd() を引数に呼ばれる | ✅ |
| 5 | REST API 利用可能確認（isAvailable）が実行される | isAvailable が1回呼ばれる | ✅ |
| 6 | discoverPort が null を返す場合、guidance を表示して process.exit(1) が呼ばれる | process.exit(1) が呼ばれ、vibe-kanban を含むガイダンスが出力される | ✅ |
| 7 | isAvailable が false を返す場合、guidance を表示して process.exit(1) が呼ばれる | process.exit(1) が呼ばれ、createProject は呼ばれない | ✅ |
| 8 | 新規プロジェクト作成後に .vibe-kanban.json が保存される | writeFileSync が `.vibe-kanban.json` と `saved-project-id` を含む内容で呼ばれる | ✅ |

### タスク 1.1.3: プロジェクト選択後の設定ファイル保存と再利用

| # | テストケース | 期待動作 | 結果 |
|---|------------|---------|------|
| 9 | 既存プロジェクトを選択すると、.vibe-kanban.json に project_id が保存される | writeFileSync が `.vibe-kanban.json` と `project-selected` を含む内容で呼ばれる | ✅ |
| 10 | 設定ファイルに有効な project_id がある場合、inquirer は表示されずに自動選択される | inquirer.prompt が呼ばれず、設定の project_id が返る | ✅ |
| 11 | 設定ファイルの project_id が有効な場合、プロジェクト名がコンソールに出力される | `Auto Project` を含むコンソール出力がある | ✅ |

### タスク 1.1.4: 無効な project_id 設定時の再選択フロー

| # | テストケース | 期待動作 | 結果 |
|---|------------|---------|------|
| 12 | 設定ファイルの project_id が存在しないプロジェクトを指している場合、警告メッセージが表示される | `invalid-project-id` を含むコンソール出力がある | ✅ |
| 13 | 設定ファイルの project_id が無効な場合、再選択を促すメッセージが表示される | `再選択` を含むコンソール出力がある | ✅ |
| 14 | 設定ファイルの project_id が無効な場合、プロジェクト選択のUIが再表示される | inquirer.prompt が呼ばれ、新たに選択した project_id が返る | ✅ |
| 15 | 設定ファイルが破損している（JSON パースエラー）場合、プロジェクト選択UIが表示される | inquirer.prompt が呼ばれ、フォールバックプロジェクトが返る | ✅ |

### その他の動作確認

| # | テストケース | 期待動作 | 結果 |
|---|------------|---------|------|
| 16 | listOrganizations がエラーをスローした場合、guidance を表示して process.exit(1) が呼ばれる | process.exit(1) が呼ばれ、vibe-kanban を含むガイダンスが出力される | ✅ |
| 17 | 組織が存在しない場合、エラーメッセージを表示して process.exit(1) が呼ばれる | process.exit(1) が呼ばれ、組織に関するエラーメッセージが出力される | ✅ |
| 18 | issueNumber を渡すと、ガイダンスメッセージに issue 番号が含まれる | ガイダンスメッセージに `123` が含まれる | ✅ |
| 19 | 複数組織がある場合、組織選択の inquirer プロンプトが表示される | orgId を name に持つプロンプトが表示され、選択した org で listProjects が呼ばれる | ✅ |

---

## 必須自動テスト結果

| 項目 | ステータス | 備考 |
|-----|----------|------|
| ユニットテスト | ✅ PASS | 19/19 テスト全パス |
| E2Eテスト | N/A | 対象外（ライブラリモジュールのテスト） |
| Lintチェック（project-selector.test.ts） | ✅ PASS | 対象ファイルのエラーなし（他ファイルの既存エラーは今回の対象外） |
| Lintチェック（パッケージ全体） | ⚠️ 既存エラー | 14件のエラー（github-client.test.ts と vibe-kanban-rest-client.ts が原因、今回のタスク対象外） |
| ビルドチェック | N/A | 今回のタスク対象外 |
| 型チェック | ✅ PASS | エラーなし（前回修正済みの型定義が正常） |

---

## 完了条件の評価

| 完了条件 | 評価 |
|---------|------|
| 全テスト（19個）がパスすること | ✅ 達成（19/19 PASS） |
| 型チェックがエラーなしで完了すること | ✅ 達成（エラーなし） |
| `project-selector.test.ts` にLintエラーがないこと | ✅ 達成（エラーなし） |
| QA仕様書が更新されていること | ✅ 達成（本ファイル更新） |

---

## QA実施ログ（再実行: 2026-02-24）

### 実施内容

前回（FAILURE）との差分：`project-selector.test.ts` の型定義を修正済み。

- **前回の問題**: `vi.spyOn()` の戻り値を `ReturnType<typeof vi.fn>` 型の変数に代入しようとして型エラーが発生
- **修正内容**: `MockInstance<typeof console.log>` / `MockInstance<typeof console.error>` 型に変更することで型エラーを解消

### テスト実行コマンドと結果

```
# ユニットテスト実行
cd packages/cli && pnpm test project-selector
→ Test Files  1 passed (1) / Tests  19 passed (19)

# 型チェック
cd packages/cli && pnpm typecheck
→ エラーなし（終了コード 0）

# project-selector ファイルのみLintチェック
pnpm exec biome lint ./src/commands/task-loop/lib/project-selector.test.ts
→ Checked 1 file in 3ms. No fixes applied.（エラーなし）

pnpm exec biome lint ./src/commands/task-loop/lib/project-selector.ts
→ Checked 1 file in 1ms. No fixes applied.（エラーなし）
```

---

## 判定: SUCCESS

**全ての受け入れ条件（AC 1.1.1〜1.1.4）および完了条件を満たしている。**

**失敗分類**: なし

**次のステップ**: 完了処理フェーズ（task-finisher）に進む
