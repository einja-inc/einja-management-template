# シンボリックリンク vs 実ファイルコピーの評価

## 📋 検証環境の必要性

### なぜ検証環境が必要か

Claude Code設定を配布する際、配布元リポジトリ自体で動作確認できる環境が必要です。

**理由**:
1. **品質保証**: 配布前にエージェント・コマンドの動作を検証
2. **開発効率**: 編集→検証のサイクルを高速化
3. **ドッグフーディング**: 自分たち自身が使うことで問題を早期発見

### 検証環境の要件

| 要件 | 詳細 |
|------|------|
| **即時反映** | エージェント・コマンドの編集が即座に検証環境に反映される |
| **実環境に近い** | 各プロジェクトで使用される状態と同じ構成 |
| **保守容易性** | 配布元と検証環境の二重管理を避ける |
| **開発者フレンドリー** | セットアップが簡単 |

---

## 🔄 2つのアプローチ

### アプローチ1: シンボリックリンク方式（推奨）

**コンセプト**: `.claude/`を`claude-code/`へのシンボリックリンクとして構成

```
einja-management-template/
├── claude-code/              # 配布元マスター（実ファイル）
│   ├── agents/
│   ├── commands/
│   └── settings.json
└── .claude/                  # 検証環境（シンボリックリンク）
    ├── agents -> ../claude-code/agents/
    ├── commands -> ../claude-code/commands/
    └── settings.json -> ../claude-code/settings.json
```

### アプローチ2: 実ファイルコピー方式

**コンセプト**: `.claude/`を`claude-code/`のコピーとして管理、同期スクリプトで更新

```
einja-management-template/
├── claude-code/              # 配布元マスター（実ファイル）
│   ├── agents/
│   ├── commands/
│   └── settings.json
├── .claude/                  # 検証環境（実ファイルコピー）
│   ├── agents/               # claude-code/agents/のコピー
│   ├── commands/             # claude-code/commands/のコピー
│   └── settings.json         # claude-code/settings.jsonのコピー
└── scripts/
    └── dev/
        └── sync.sh           # 同期スクリプト（必須）
```

---

## ⭐ アプローチ1: シンボリックリンク方式（詳細評価）

### セットアップ手順

```bash
# 1. 既存.claude/を削除
rm -rf .claude

# 2. シンボリックリンク作成
mkdir -p .claude
ln -s ../claude-code/agents .claude/agents
ln -s ../claude-code/commands .claude/commands
ln -s ../claude-code/settings.json .claude/settings.json

# 3. 確認
ls -la .claude/
# lrwxr-xr-x  agents -> ../claude-code/agents
# lrwxr-xr-x  commands -> ../claude-code/commands
# lrwxr-xr-x  settings.json -> ../claude-code/settings.json

# 4. .gitattributes設定（重要）
cat >> .gitattributes <<EOF
# シンボリックリンクをそのままgitで管理
.claude/** symlinks=true
EOF

# 5. git config設定（開発者向け）
git config core.symlinks true
```

### 開発フロー

```bash
# 1. エージェントを編集
vi claude-code/agents/task/task-executer.md

# 2. 即座に.claude/に反映される（確認）
cat .claude/agents/task/task-executer.md  # ✅ 変更が反映されている

# 3. Claude Codeで検証
claude "/task-exec #123"

# 4. 問題なければコミット
git add claude-code/agents/task/task-executer.md
git commit -m "feat: task-executer の修正"
```

---

### メリット

| 項目 | 評価 | 詳細 |
|------|------|------|
| **開発効率** | ⭐⭐⭐⭐⭐ | 編集→即反映、検証サイクルが高速 |
| **保守性** | ⭐⭐⭐⭐⭐ | マスターファイルは1箇所のみ、同期忘れリスクゼロ |
| **検証容易性** | ⭐⭐⭐⭐⭐ | 配布するエージェントをそのままテスト可能 |
| **ディスク容量** | ⭐⭐⭐⭐⭐ | ファイル実体は1つだけ、容量節約 |
| **Git管理** | ⭐⭐⭐⭐ | `.gitattributes`設定で問題なし |

### デメリット

| 項目 | 評価 | 詳細 |
|------|------|------|
| **クロスプラットフォーム** | ⭐⭐⭐ | Windows非WSLでは代替手段が必要 |
| **学習コスト** | ⭐⭐⭐ | シンボリックリンクの概念理解が必要 |
| **Git設定** | ⭐⭐⭐⭐ | `.gitattributes`と`core.symlinks`の設定が必要 |

---

### クロスプラットフォーム対応

#### Mac / Linux / Windows WSL（推奨環境）

**セットアップ**:
```bash
# gitでシンボリックリンクを有効化
git config core.symlinks true

# クローン
git clone https://github.com/org/einja-management-template.git
cd einja-management-template

# 自動的にシンボリックリンクとして展開される
ls -la .claude/
```

**動作**: ✅ 問題なし

---

#### Windows（非WSL）

**問題**: シンボリックリンクが正しく機能しない可能性

**対策1: 開発者モードを有効化**
```powershell
# Windows 10/11の開発者モードを有効化
# 設定 > 更新とセキュリティ > 開発者向け > 開発者モード

# その後、git configを設定
git config --global core.symlinks true
```

**対策2: 代替スクリプト使用**
```bash
# scripts/dev/sync-to-dotclaude.sh を使用
./scripts/dev/sync-to-dotclaude.sh

# ファイル監視自動同期版
./scripts/dev/watch-and-sync.sh &
```

**対策3: WSLの使用を推奨**
```bash
# WSL2をインストール
wsl --install

# WSL内で作業
wsl
cd /mnt/c/projects/einja-management-template
```

---

### Git設定の詳細

#### .gitattributes

```
# .gitattributes
# シンボリックリンクをそのままgitで管理
.claude/** symlinks=true
```

**効果**:
- シンボリックリンクがテキストファイルとして展開されるのを防ぐ
- git cloneでシンボリックリンクとして正しく復元される

#### core.symlinks設定

```bash
# プロジェクトローカルで設定
git config core.symlinks true

# またはグローバルで設定
git config --global core.symlinks true
```

**効果**:
- gitがシンボリックリンクを認識する
- `git add`でシンボリックリンク情報が保存される

---

## 📦 アプローチ2: 実ファイルコピー方式（詳細評価）

### セットアップ手順

```bash
# 1. .claude/を実ファイルとして作成
mkdir -p .claude
cp -r claude-code/agents .claude/
cp -r claude-code/commands .claude/
cp claude-code/settings.json .claude/

# 2. 同期スクリプトを用意
cat > scripts/dev/sync-to-dotclaude.sh <<'EOF'
#!/bin/bash
set -e
echo "🔄 claude-code/ → .claude/ に同期中..."
cp -r claude-code/agents .claude/
cp -r claude-code/commands .claude/
cp claude-code/settings.json .claude/
echo "✅ 同期完了"
EOF

chmod +x scripts/dev/sync-to-dotclaude.sh
```

### 開発フロー

```bash
# 1. エージェントを編集
vi claude-code/agents/task/task-executer.md

# 2. 同期スクリプト実行（必須）
./scripts/dev/sync-to-dotclaude.sh

# 3. Claude Codeで検証
claude "/task-exec #123"

# 4. 問題なければコミット
git add claude-code/agents/task/task-executer.md
git commit -m "feat: task-executer の修正"
```

---

### メリット

| 項目 | 評価 | 詳細 |
|------|------|------|
| **クロスプラットフォーム** | ⭐⭐⭐⭐⭐ | すべての環境で問題なし |
| **Git扱い** | ⭐⭐⭐⭐⭐ | 通常のファイルとして扱える |
| **学習コスト** | ⭐⭐⭐⭐⭐ | 特別な知識不要 |

### デメリット

| 項目 | 評価 | 詳細 |
|------|------|------|
| **開発効率** | ⭐⭐ | 編集後に同期スクリプト実行が必要 |
| **保守性** | ⭐⭐ | 二重管理、同期忘れリスク |
| **検証容易性** | ⭐⭐⭐ | 同期手順が追加で必要 |
| **ディスク容量** | ⭐⭐⭐ | ファイルが重複 |

---

### 同期忘れのリスク

**問題**: 開発者が`claude-code/`を編集しても`.claude/`の更新を忘れる

**影響**:
- 検証時に古いエージェント・コマンドが使用される
- バグを見逃す可能性
- 配布時に期待と異なる動作

**対策**:
```bash
# 1. ファイル監視自動同期スクリプト
cat > scripts/dev/watch-and-sync.sh <<'EOF'
#!/bin/bash
# fswatch（Mac）またはinotify-tools（Linux）を使用

fswatch -o claude-code/ | while read change; do
  echo "🔄 変更検出: 自動同期中..."
  ./scripts/dev/sync-to-dotclaude.sh
done
EOF

# 2. Pre-commit hookで自動同期
cat > .git/hooks/pre-commit <<'EOF'
#!/bin/bash
# claude-code/が変更されている場合、.claude/を自動同期
if git diff --cached --name-only | grep -q "^claude-code/"; then
  echo "🔄 claude-code/ の変更を検出: .claude/ を自動同期中..."
  ./scripts/dev/sync-to-dotclaude.sh
  git add .claude/
fi
EOF

chmod +x .git/hooks/pre-commit
```

---

## 📊 詳細比較表

### 開発体験

| 項目 | シンボリックリンク | 実ファイルコピー |
|------|------------------|---------------|
| **編集→検証** | 即座 | 同期スクリプト実行後 |
| **同期忘れリスク** | なし | あり（対策必要） |
| **開発フロー** | シンプル（編集→検証） | 複雑（編集→同期→検証） |
| **ファイル監視** | 不要 | 推奨 |

### 技術的側面

| 項目 | シンボリックリンク | 実ファイルコピー |
|------|------------------|---------------|
| **ディスク容量** | 節約（実体1つ） | 重複（実体2つ） |
| **Git管理** | `.gitattributes`設定必要 | 標準的な扱い |
| **バックアップ** | 1箇所 | 2箇所（非効率） |

### プラットフォーム対応

| 環境 | シンボリックリンク | 実ファイルコピー |
|------|------------------|---------------|
| **Mac** | ✅ ネイティブ対応 | ✅ 対応 |
| **Linux** | ✅ ネイティブ対応 | ✅ 対応 |
| **Windows WSL** | ✅ 対応 | ✅ 対応 |
| **Windows（非WSL）** | ⚠️ 開発者モード必要 | ✅ 完全対応 |

---

## 🎯 推奨: シンボリックリンク方式

### 推奨理由

#### 1. 圧倒的な開発効率

**シンボリックリンク**:
```bash
vi claude-code/agents/frontend-coder.md  # 編集
claude "/frontend-implement ログイン"      # 即座に検証
# → 2ステップ
```

**実ファイルコピー**:
```bash
vi claude-code/agents/frontend-coder.md  # 編集
./scripts/dev/sync-to-dotclaude.sh       # 同期（必須）
claude "/frontend-implement ログイン"      # 検証
# → 3ステップ（同期忘れリスクあり）
```

**効果**: 開発サイクルが**30%高速化**（推定）

#### 2. 同期忘れゼロ

**シンボリックリンク**: マスターファイルは1箇所のみ、同期という概念がない
**実ファイルコピー**: 二重管理、同期忘れで検証ミスのリスク

#### 3. チーム開発の現実

**開発チームの環境**（一般的な統計）:
- Mac: 50-60%
- Linux: 20-30%
- Windows WSL: 10-20%
- Windows（非WSL）: 5-10%

**結論**: 開発者の90%以上がシンボリックリンクをネイティブサポート

#### 4. 保守コストの削減

- ファイル重複なし
- 同期スクリプト不要
- ファイル監視スクリプト不要
- Pre-commit hook不要

---

### Windows非WSLユーザーへの対応

**基本方針**: WSL使用を推奨

**理由**:
- Windows開発環境の標準
- Node.js、pnpm、Dockerなど多くのツールがWSLを推奨
- Claude Codeもターミナル環境を想定

**代替手段**:
1. **開発者モード有効化**: Windows 10/11の設定で有効化
2. **同期スクリプト**: `scripts/dev/sync-to-dotclaude.sh`を提供
3. **ファイル監視**: `scripts/dev/watch-and-sync.sh`で自動同期

---

## 📝 README.mdへの記載内容

```markdown
## 開発者向け: このリポジトリでの作業方法

### 前提条件

このリポジトリは**Claude Code設定の配布元**であり、同時に**検証環境**としても機能します。

### シンボリックリンク設定

#### Mac / Linux / Windows WSL（推奨）

```bash
# gitでシンボリックリンクを有効化
git config core.symlinks true

# クローン（既にクローン済みの場合は再クローン推奨）
git clone https://github.com/org/einja-management-template.git
cd einja-management-template

# シンボリックリンクが正しく作成されているか確認
ls -la .claude/
# → agents, commands, settings.json がシンボリックリンクになっている
```

#### Windows（非WSL）

**推奨**: WSL2のインストール
```bash
wsl --install
```

**代替手段**: 同期スクリプト使用
```bash
# エージェント編集後に実行
./scripts/dev/sync-to-dotclaude.sh

# または自動監視
./scripts/dev/watch-and-sync.sh &
```

### 開発フロー

1. **配布元を編集**: `claude-code/agents/` または `claude-code/commands/` を編集
2. **即座に反映**: シンボリックリンクにより `.claude/` に自動反映（非WSL Windowsは手動同期）
3. **Claude Codeで検証**: このリポジトリでClaude Codeを実行して動作確認
4. **コミット**: `claude-code/` 配下のファイルをコミット

### ディレクトリ役割

| ディレクトリ | 役割 | 編集対象 |
|------------|------|---------|
| `claude-code/` | **配布元マスター** | ✅ エージェント・コマンド開発時はここを編集 |
| `.claude/` | **検証用（シンボリックリンク）** | ❌ 直接編集しない（自動反映） |
| `docs-templates/` | **ドキュメントテンプレート** | ✅ テンプレート開発時はここを編集 |
| `docs/` | **カスタマイズ例** | ⚠️ このリポジトリ用の例として編集 |
```

---

## ✅ 最終推奨

**シンボリックリンク方式を採用**

**理由**:
1. ✅ 開発効率が圧倒的に高い
2. ✅ 保守性が高い（同期忘れゼロ）
3. ✅ 開発チームの90%以上がネイティブサポート環境
4. ✅ ディスク容量節約
5. ✅ Windows非WSLは代替手段で対応可能

**懸念への対策**:
- Windows非WSL: WSL推奨 + 代替スクリプト提供
- Git設定: README.mdで明確に手順を記載
- 学習コスト: シンプルな概念、セットアップ手順明記

---

**前のドキュメント**: [実装計画](./03-implementation-plan.md)
**インデックスに戻る**: [README.md](./README.md)
