# QAテスト仕様書: 3.2 - issue_id パラメータのオプション化

## テスト概要
- **タスクグループID**: 3.2
- **テスト対象**: `start_workspace_session` の `issue_id` パラメータのオプション化
- **テスト実施日**: 2026-02-16
- **テスト実施者**: QA Agent (task-qa)

## 対象受け入れ基準

### AC3.2: start_workspace_session の新パラメータ
- Given: 有効なIssue ID、executor、reposが存在
- When: `startTaskAttempt(taskId, executor, repos)` を呼び出す
- Then: `start_workspace_session` に `title`（必須）、`executor`（必須）、`repos`（必須）、`issue_id`（オプション）のパラメータを渡し、Attemptが開始される
- 検証レベル: Integration

## テストケース

### TC3.2.1: issue_id 指定時のパラメータ構造 ✅
- **説明**: `issue_id` を指定した場合、全パラメータが正しく渡される
- **前提条件**: vibe-kanban MCPが接続済み
- **テスト手順**:
  1. `startTaskAttempt(title, executor, repos, issueId)` を呼び出す
  2. MCP呼び出しパラメータを確認
- **期待結果**: `start_workspace_session` に `{ title, executor, repos, issue_id }` が渡される
- **実施結果**: ✅ PASS
- **確認方法**: ユニットテスト（vibe-kanban-client.test.ts line 369-411）
- **エビデンス**: テスト成功（15/15 tests passed）

### TC3.2.2: issue_id 未指定時のパラメータ構造 ✅
- **説明**: `issue_id` を指定しない場合、パラメータから除外される
- **前提条件**: vibe-kanban MCPが接続済み
- **テスト手順**:
  1. `startTaskAttempt(title, executor, repos)` を呼び出す（issueId なし）
  2. MCP呼び出しパラメータを確認
- **期待結果**: `start_workspace_session` に `{ title, executor, repos }` のみが渡される（issue_id は含まれない）
- **実施結果**: ✅ PASS
- **確認方法**: ユニットテスト（vibe-kanban-client.test.ts line 413-）
- **エビデンス**: テスト成功（15/15 tests passed）

### TC3.2.3: 必須パラメータの検証 ✅
- **説明**: title、executor、repos が必須パラメータとして機能する
- **前提条件**: TypeScriptプロジェクト
- **テスト手順**:
  1. `startTaskAttempt` の型定義を確認
  2. ビルド・型チェックを実行
- **期待結果**: 必須パラメータが欠けた場合、TypeScriptエラーが発生
- **実施結果**: ✅ PASS
- **確認方法**: 型チェック（`pnpm typecheck`）
- **エビデンス**: 型エラー0件

## 必須自動テスト結果

| テスト項目 | コマンド | 結果 | 備考 |
|----------|---------|------|------|
| ユニットテスト | `pnpm test` | ✅ PASS | 15/15 tests passed |
| Lintチェック | `pnpm lint` | ✅ PASS | エラー0件 |
| ビルド | `pnpm build` | ✅ PASS | ビルド成功 |
| 型チェック | `pnpm typecheck` | ✅ PASS | 型エラー0件 |
| フォーマット | `pnpm format:fix` | ✅ PASS | フォーマット適用済み |

## テストサマリー

| 項目 | 値 |
|-----|-----|
| 総テストケース数 | 3 |
| 成功 | 3 |
| 失敗 | 0 |
| 部分成功 | 0 |
| 成功率 | 100% |

## 検出された問題

なし

## 備考

- タスク3.1とタスク3.2は同じコード変更をテストするため、必須自動テストの結果は同一
- `issue_id` のオプショナル化は既存のユニットテストで十分にカバーされている
- 型安全性が確保され、TypeScriptコンパイラによって必須パラメータの検証が行われる

## テスト結果: ✅ SUCCESS

全ての受け入れ基準を満たしています。
