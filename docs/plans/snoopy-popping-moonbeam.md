# 外部API連携時の実装前打鍵確認ルールを全エージェントに適用

## Context

OpenAI Realtime API（Beta→GA移行）で、specの読みが甘いまま推測でスキーマを実装した結果、format型（string→object）、modalitiesキー名、audioネスト構造など10回以上の修正が連続。毎回デプロイ→エラー→修正の不毛なループが発生した。

**目的**: 外部APIを叩くコードを書く・修正する場合、実装前に必ずAPI打鍵（curl/WebSocket等）で正しいリクエスト/レスポンス形式を確認するルールを、全開発者・全エージェントに適用する。

## 現状

- `task-executer.md`: 4.5「形骸化実装の禁止」まであるが、外部API打鍵確認ルールなし
- `einja-task-exec/SKILL.md`: Step 1.5「環境前提条件チェック」で環境変数の有無チェックのみ（警告のみ、打鍵確認なし）
- `einja-review-code/SKILL.md`: 観点C「セキュリティ・エラーハンドリング」に外部入力の記述はあるが、打鍵確認済みかのレビュー項目なし

## 変更内容

### 1. `task-executer.md` — セクション4.6追加（実装エージェント本体）

`.claude/agents/einja/task/task-executer.md` の `4.5 形骸化実装の禁止` の直後に `4.6 外部API連携時の実装前打鍵確認` を追加:

```markdown
#### 4.6 外部API連携時の実装前打鍵確認

**⚠️ 外部APIを叩くコードを新規作成・修正する場合、実装前に必ず以下を実行すること**:

1. **API打鍵テスト**: curl、WebSocket接続、またはスクリプトで実際にAPIを叩き、正しいリクエスト/レスポンス形式を確認する
2. **スキーマ確認**: 公式ドキュメントやOpenAPI specを読むだけでなく、実際のレスポンスボディを目視確認する
3. **確認してから実装**: 打鍵結果に基づいてコードを書く。推測やドキュメントの斜め読みで実装しない

**適用範囲**:
- 新しい外部APIの初期実装
- 既存API連携のバージョンアップ（Beta→GA等）
- APIパラメータの追加・変更

**実装前セルフチェック**:
- [ ] APIエンドポイントにリクエストを送り、成功レスポンスを取得したか？
- [ ] レスポンスの実際の構造（フィールド名、型、ネスト）を確認したか？
- [ ] エラーレスポンスのフォーマットも確認したか？
```

### 2. `einja-task-exec/SKILL.md` — Step 4のプロンプトに外部API検出フラグ追加

Step 1.5の環境チェック結果を活用し、Step 4でtask-executerに渡すプロンプトに「外部API連携あり」の情報を追加:

Step 1.5の末尾に追記:
```markdown
4. **外部API連携フラグ**: 外部サービス用環境変数が検出された場合、`外部API連携あり` フラグを記録する。このフラグはStep 4でtask-executerのプロンプトに含めて渡す
```

Step 4のプロンプト項目（a〜g）に追加:
```markdown
       h. 外部API連携フラグ（Step 1.5で検出された場合）→ 「⚠️ このタスクは外部API連携を含みます。実装前にAPI打鍵テスト（curl等）で正しいリクエスト/レスポンス形式を確認してから実装してください（task-executer 4.6参照）」
```

### 3. `einja-review-code/SKILL.md` — 観点Cにレビュー項目追加

観点C「セキュリティ・エラーハンドリング・異常系」の説明を拡張:

現在:
```
| C | セキュリティ・エラーハンドリング・異常系 | OWASP Top 10、入力バリデーション、エラー伝播、異常系パス | API・認証・外部入力・DB操作等の変更がある場合 |
```

変更後:
```
| C | セキュリティ・エラーハンドリング・異常系 | OWASP Top 10、入力バリデーション、エラー伝播、異常系パス、外部APIスキーマ整合性 | API・認証・外部入力・DB操作等の変更がある場合 |
```

観点Cのレビュープロンプト（サブエージェントに渡す指示）に以下を追加:
```
- 外部API連携コードがある場合: リクエスト/レスポンスのスキーマが公式ドキュメントと一致しているか確認。特にフィールド名、型（string vs object）、ネスト構造、必須/オプションの区別
```

## タスク概要

| ID | タスク | Skill/サブエージェント | 依存 |
|----|--------|----------------------|------|
| 0-0 | TaskCreate一括登録 | - | - |
| 0-1 | Planファイルリネーム | - | - |
| 1-1 | task-executer.mdにセクション4.6追加 [`general-purpose`] | なし |
| 1-2 | einja-task-exec/SKILL.mdにStep 1.5拡張 + Step 4プロンプト追加 [`general-purpose`] | なし |
| 1-3 | einja-review-code/SKILL.mdに観点C拡張 [`general-purpose`] | なし |
| 99-1 | 観点別並列コードレビュー [`einja-review-code`] | 1-1〜1-3 |
| 99-G | コミット承認ゲート [`AskUserQuestion`] | 99-1 |
| 99-3 | コミット・プッシュ [`einja-task-commit`] | 99-G |

## 並列実行計画

- タスク1-1, 1-2, 1-3は互いに独立。3つ並列実行可能

## リスク・不明点

- なし（既存セクションへの追記のみで、既存機能への影響なし）

## 検証・動作確認方法

- 各ファイルの変更箇所をgrep/readで確認
- task-executer.mdの4.6セクションが4.5の直後に配置されていること
- einja-task-exec/SKILL.mdのStep 1.5とStep 4に外部APIフラグが追加されていること
- einja-review-code/SKILL.mdの観点Cテーブルにスキーマ整合性が追加されていること
