# Plan: CLAUDE.mdのPlanモードフロー強化

## Context
1. 新しい依頼時に過去Planを検索して類似対応を再利用する仕組みがない
2. Planモードの各フェーズで使うSkillやTaskAPIの使い方が明文化されていない
3. TaskCreate時のタスク概要にSkill名が含まれないため、タスクを見ただけでは何のSkillを使うか分からない
4. Planファイルに何を書くべきかが定義されていない

## 対象ファイル
- `CLAUDE.md`

## 変更1: 基本原則6の拡張（L13）

末尾に追記: **「過去Planに類似対応がある場合はSkill化を強く推奨」**

## 変更2: Planモード時の必須フローを拡充（L69-78）

5ステップに拡張し、各ステップに使用Skill/APIを `[]` で明記:

1. 問題・要件を調査・分析する [`Explore` サブエージェント]
2. `docs/plans/` の過去Planを検索し、類似対応がないか確認する [`Grep`/`Glob`]
   - 類似Planがあれば知見を計画に取り入れる
   - 類似Planの存在はステップ3でSkill化の強い根拠とする
3. `einja-skill-first` で「Skill を先に作るべきか」を評価する [`einja-skill-first`]
   - （既存の説明をそのまま維持）
4. 実装で使用するSkill・サブエージェントを選定する
   - サブエージェント委託ルール表と照合し、各作業に適切なSkill/サブエージェントを割り当てる
   - 選定結果はplanファイルの「使用予定Skill・サブエージェント」セクションに記載する
5. planファイルに計画を記述する
6. ExitPlanMode で承認を得る

## 変更3: Planファイルの必須セクション定義を追加

「Planモード時の必須フロー」の後に新セクションを追加:

```
### Planファイルの必須セクション
- **Context**: なぜこの変更が必要か
- **変更内容**: 推奨アプローチのみ記載（対象ファイルパス含む）
- **使用予定Skill・サブエージェント**: 実装で使用するSkill名・サブエージェント種別のリスト
- **検証方法**: 変更をどう検証するか
```

## 変更4: TaskCreate時の概要記述ルールを追加

「計画・進捗管理の規約」セクションに追記:

```
### TaskCreate タスク概要の記述ルール
- タスク概要には使用するSkill名を `[Skill名]` 形式で含める
- 例: `「過去Plan検索 [Grep/Glob]」` `「Skill-First評価 [einja-skill-first]」`
```

## 検証
- CLAUDE.md の変更を目視確認
- ステップ番号の整合性を確認
